''' the build is a python file because I'm kinda
more comfortable with it than c++
but using c++ instead of python here is 
not debatable.
first:
c++ compiles directly to an .exe which is perfect to what i want,
letting people use qc right out of the box
and the python file is only used for building during development;
regular users never have to touch python.

'''

import subprocess
import sys
import platform # silly thing

class build_qc:
    # i have been corrupted with oop
    # yes it's completely unnecessary to use classes for this ik so quirky right
    def __init__(self):
        print("initiating build qc...")
        #idk what to put here lol

def main ()
if platform.system() != "Windows":
    print('lil jit tweaking if u think you can run this on something other than windows')
    print(" why isn't this supported on non-windows os's? oh I just don't know how to code for them lol")
    sys.exit(67)

